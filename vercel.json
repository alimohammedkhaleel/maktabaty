{
  "buildCommand": "cd client/my-react-app && npm install && npm run build",
  "outputDirectory": "client/my-react-app/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
